name: Tests

# One ctest suite across the compiler matrix. Called once per suite so core and
# cli report as separate checks and fail independently.
on:
  workflow_call:
    inputs:
      suite:
        description: 'ctest suite to run: core or cli'
        required: true
        type: string
      build-cli:
        description: 'Build the CLI. Off for the core suite, which then doubles as proof that the library stands on its own.'
        required: false
        type: boolean
        default: true

permissions:
  contents: read

# A called workflow cannot see the caller's env, so this is repeated per file.
env:
  APT_PACKAGES: >-
    build-essential
    cmake
    pkg-config
    libsodium-dev
    libjpeg-dev

jobs:
  test:
    name: ${{ inputs.suite }} (${{ matrix.os }}, ${{ matrix.cc }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04]
        cc: [gcc, clang]
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends $APT_PACKAGES ${{ matrix.cc }}

      - name: Configure
        run: |
          cmake -S . -B build \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER=${{ matrix.cc }} \
            -DHH_WARNINGS_AS_ERRORS=ON \
            -DHH_BUILD_CLI=${{ inputs.build-cli && 'ON' || 'OFF' }}

      - name: Build
        run: cmake --build build -j"$(nproc)"

      - name: Test
        run: ctest --test-dir build -R '^${{ inputs.suite }}$' --output-on-failure

      # The suite's own e2e tests drive hh against generated images. This runs it
      # against the real assets instead, so the committed PNG and JPEG stay
      # decodable and the JPEG/DCT path gets a non-synthetic carrier.
      - name: Round trip
        if: inputs.suite == 'cli'
        run: |
          set -euo pipefail

          head -c 4096 /dev/urandom > payload.bin
          head -c 2048 /dev/urandom > small.bin

          for codec in lsbr lsbm; do
            for phrase in "correct horse battery staple" ""; do
              printf '%s\n' "$phrase" | ./build/hh encode assets/hush.png payload.bin -o "carrier-$codec.png" -c "$codec"
              printf '%s\n' "$phrase" | ./build/hh decode "carrier-$codec.png" -o "recovered-$codec.bin"
              ./devtools/checksum.sh payload.bin "recovered-$codec.bin"

              printf '%s\n' "$phrase" | ./build/hh encode assets/hush.jpg small.bin -o "carrier-$codec.jpg" -c "$codec"
              printf '%s\n' "$phrase" | ./build/hh decode "carrier-$codec.jpg" -o "recovered-jpeg-$codec.bin"
              ./devtools/checksum.sh small.bin "recovered-jpeg-$codec.bin"
            done
          done
